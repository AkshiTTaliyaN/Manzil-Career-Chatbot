# Bug 3 fix: DEMO_MODE is now controlled here, not hardcoded in config.js
# Remove this line (or set to false) to connect to the real backend.
VITE_DEMO_MODE=true

# Backend API URL (defaults to http://localhost:8000 if not set)
VITE_API_URL=http://localhost:8000